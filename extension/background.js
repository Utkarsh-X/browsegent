// Background service worker — Phase 5 stub
// Agent loop stays in Node.js for Phase 5
// This file exists to satisfy MV3 requirements

chrome.runtime.onInstalled.addListener(() => {
  console.log('[BrowseGent] Extension installed');
});
